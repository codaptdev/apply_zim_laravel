@props(['value', 'title'])

<div class="w-full bg-slate-100 p-3  flex flex-col justify-center items-center rounded-xl h-full ">
    <p class="font-medium text-slate-400 text-xl">{{$title}}</p>
    <h1 class="text-slate-600" >{{$value}}</h1>
</div>
